<%
def url(page, params = nil)
  @base_url + "?page=" + page + (params ? "&#{params}" : "")
end

zone = @object
-%>
Ahoj <%= @user.login %>,

nahlášený problém s připraveností přímého přenosu DNS zóny <%= zone.name %>
z tvých nakonfigurovaných primárních DNS serverů na naše sekundární DNS servery
je vyřešený.

Záznamy přenosů zóny najdeš ve vpsAdminu:

  <%= url('dns', "action=transfer_log&dns_zone=#{zone.id}") %>

Díky.

S pozdravem

tým vpsFree.cz

(Tento mail automaticky rozesílá vpsAdmin, Tvoje odpověď se zašle na naši podporu)
