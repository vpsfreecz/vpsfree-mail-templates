<%
def url(page, params = nil)
  @base_url + "?page=" + page + (params ? "&#{params}" : "")
end

zone = @object
-%>
Hi <%= @user.login %>,

the reported problem with direct transfer readiness for DNS zone
<%= zone.name %> from your configured primary DNS servers to our secondary DNS
servers has been resolved.

You can view zone transfer logs in vpsAdmin:

  <%= url('dns', "action=transfer_log&dns_zone=#{zone.id}") %>

Thank you.

Best regards,

vpsFree.cz team

(This is an automated mail from vpsAdmin, your reply will be sent to our support)
