Hi <%= @dataset.user.login %>,

<% if @src_pool.node_id == @dst_pool.node_id -%>
dataset <%= @dataset.full_name %> will be transfered from pool <%= @src_pool.name %> to pool <%= @dst_pool.name %> on <%= @src_pool.node.domain_name %>.
<% else -%>
dataset <%= @dataset.full_name %> will be transfered from <%= @src_pool.node.domain_name %> to <%= @dst_pool.node.domain_name %>.
<% end -%>
<% if @exports.any? -%>

The following exports will be affected:

<% @exports.each do |export| -%>
 - <%= export.host_ip_address.ip_addr %>:<%= export.path %>
<% end -%>

NFS servers for exports are going to be turned off and on during the transfer.
<% end -%>
<% if @maintenance_window -%>
<% days = %i(Sun Mon Tue Wed Thu Fri Sat) -%>

The transfer will be finished within the closest possible maintenance window:

<% @maintenance_windows.each do |w| -%>
  - <%= days[w.weekday] %> from <%= sprintf('%02d:%02d', w.opens_at / 60, w.opens_at % 60) %> to <%= sprintf('%02d:%02d', w.closes_at / 60, w.closes_at % 60) %>
<% end -%>

<% elsif @custom_window -%>
<% days = %i(Sunday Monday Tuesday Wednesday Thursday Friday Saturday) -%>

The transfer will be finished on <%= days[@finish_weekday] %> from <%= sprintf('%02d:%02d', @finish_minutes / 60, @finish_minutes % 60) %>.
<% end -%>
<% if @reason -%>

Reason: <%= @reason %>
<% end -%>

Best regards,

vpsFree.cz team

(This is an automated mail from vpsAdmin, your reply will be sent to our support)
