Ahoj <%= @r.login %>,

tvá přihláška není správně vyplněná a zatím tak nemůže být schválena.

<% if @r.admin_response %>Důvod: <%= @r.admin_response %><% end %>

Přihlášku můžeš opravit a znovu odeslat na následující adrese:

  https://vpsfree.cz/prihlaska/<%= (@r.org_id && !@r.org_id.empty?) ? 'pravnicka' : 'fyzicka' %>-osoba/?<%= @r.id %>:<%= @r.access_token %>

S pozdravem

vpsAdmin @ vpsFree.cz
