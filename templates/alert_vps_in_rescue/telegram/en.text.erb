VPS <%= @vps.hostname %> (#<%= @vps.id %>): rescue mode

State: running in rescue mode
Please return the VPS to normal boot when you are done.
<% vps_url = webui_url("?page=adminvps&action=info&veid=#{@vps.id}") -%>
<% if vps_url.present? -%>

Link: <%= vps_url %>
<% end -%>
