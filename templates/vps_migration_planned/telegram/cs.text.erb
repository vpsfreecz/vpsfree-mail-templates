VPS <%= @vps.hostname %> (#<%= @vps.id %>): přesun naplánován

Z: <%= @src_node.domain_name %>
Na: <%= @dst_node.domain_name %>
Okno: <%= @m.maintenance_window ? 'servisní okno' : 'naplánováno administrátory' %>
<% reason = @reason || (@m.respond_to?(:migration_plan) && @m.migration_plan&.reason) -%>
<% if reason.present? -%>
Důvod: <%= reason %>
<% end -%>
<% vps_url = webui_url("?page=adminvps&action=info&veid=#{@vps.id}") -%>
<% if vps_url.present? -%>

Odkaz: <%= vps_url %>
<% end -%>
