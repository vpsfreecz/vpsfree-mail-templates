VPS <%= @vps.hostname %> (#<%= @vps.id %>): migration planned

From: <%= @src_node.domain_name %>
To: <%= @dst_node.domain_name %>
Window: <%= @m.maintenance_window ? 'maintenance window' : 'scheduled by administrators' %>
<% reason = @reason || (@m.respond_to?(:migration_plan) && @m.migration_plan&.reason) -%>
<% if reason.present? -%>
Reason: <%= reason %>
<% end -%>
<% vps_url = webui_url("?page=adminvps&action=info&veid=#{@vps.id}") -%>
<% if vps_url.present? -%>

Link: <%= vps_url %>
<% end -%>
