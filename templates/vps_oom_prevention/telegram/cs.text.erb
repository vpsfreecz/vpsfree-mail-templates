VPS <%= @vps.hostname %> (#<%= @vps.id %>): prevence OOM

OOM: <%= @ooms_in_period %> za posledních <%= @period_seconds.to_i / 60 %> minut
Akce: <%= @action == :restart ? 'restartováno' : 'vypnuto' %>
Zkontroluj prosím využití paměti ve VPS.
<% vps_url = webui_url("?page=adminvps&action=info&veid=#{@vps.id}") -%>
<% if vps_url.present? -%>

Odkaz: <%= vps_url %>
<% end -%>
