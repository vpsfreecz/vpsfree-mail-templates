VPS <%= @vps.hostname %> (#<%= @vps.id %>): OOM prevention

OOMs: <%= @ooms_in_period %> in last <%= @period_seconds.to_i / 60 %> minutes
Action: <%= @action == :restart ? 'restarted' : 'shut down' %>
Please inspect memory usage in the VPS.
<% vps_url = webui_url("?page=adminvps&action=info&veid=#{@vps.id}") -%>
<% if vps_url.present? -%>

Link: <%= vps_url %>
<% end -%>
