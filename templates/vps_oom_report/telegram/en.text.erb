VPS <%= @vps.hostname %> (#<%= @vps.id %>): out of memory reports

Detected OOMs: <%= @all_oom_count %>
Shown OOMs: <%= @selected_oom_count %>
<% @selected_oom_reports.each do |r| -%>
- <%= local_time(r.created_at, '%Y-%m-%d %H:%M:%S %Z') %>: <%= r.killed_name ? "Killed #{r.killed_name}" : 'Nothing killed' %>, <%= r.count %>x
<% end -%>
<% if @all_oom_count != @selected_oom_count -%>
More OOMs: <%= @all_oom_count - @selected_oom_count %>
<% end -%>
<% oom_url = webui_url("?page=oom_reports&vps=#{@vps.id}&list=1") -%>
<% if oom_url.present? -%>

Link: <%= oom_url %>
<% end -%>
<% if defined?(@mute_url) && @mute_url.present? -%>

Mute similar OOM reports: <%= @mute_url %>
<% end -%>
