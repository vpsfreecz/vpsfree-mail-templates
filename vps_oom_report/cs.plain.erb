Ahoj <%= @vps.user.login %>,

ve VPS <%= @vps.id %> <%= @vps.hostname %> dochází paměť.

Bylo zaznamenáno <%= @oom_reports.count %> případů vyčerpání paměti:

<% @oom_reports.each do |r| -%>
  <%= r.created_at.localtime.strftime('%Y-%m-%d %H:%M:%S %Z') %>  <%= r.killed_name ? "Ukončen #{r.killed_name}" : 'Nic neukončeno' %>
<% end -%>

Pro více informací viz

  <%= @base_url %>?page=oom_reports&vps=<%= @vps.id %>&list=1

S pozdravem,

vpsAdmin @ vpsFree.cz
