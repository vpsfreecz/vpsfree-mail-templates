Hi <%= @vps.user.login %>,

VPS <%= @vps.id %> <%= @vps.hostname %> is running out of memory.

Detected <%= @oom_reports.count %> out of memory situations:

<% @oom_reports.each do |r| -%>
  <%= r.created_at.localtime.strftime('%Y-%m-%d %H:%M:%S %Z') %>  <%= r.killed_name ? "Killed #{r.killed_name}" : "Nothing killed" %>
<% end -%>

Please see

  <%= @base_url %>?page=oom_reports&vps=<%= @vps.id %>&list=1

for more information.

Best regards,

vpsAdmin @ vpsFree.cz
