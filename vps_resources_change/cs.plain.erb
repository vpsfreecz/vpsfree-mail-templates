Ahoj <%= @vps.user.login %>,

došlo ke změně konfigurace VPS <%= @vps.id %> <%= @vps.hostname %>.

Aktuální limity:
    CPU: <%= @vps.cpu %> jader, limit <%= @vps.cpu_limit || (@vps.cpu * 100) %> %
  Paměť: <%= @vps.memory %> MB
   Swap: <%= @vps.swap %> MB

Důvod změny: <%= @reason %>

Změnu provedl: <%= @admin.full_name %>

S pozdravem

tým vpsFree.cz
