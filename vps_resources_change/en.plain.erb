Hi <%= @vps.user.login %>,

configuration of VPS <%= @vps.id %> <%= @vps.hostname %> has changed.

Current limits:
    CPU: <%= @vps.cpu %> cores, limit <%= @vps.cpu_limit || (@vps.cpu * 100) %> %
 Memory: <%= @vps.memory %> MB
   Swap: <%= @vps.swap %> MB

Reason for change: <%= @reason %>

Changed by: <%= @admin.full_name %>

Best regards,

vpsFree.cz team
